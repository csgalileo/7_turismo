<h1>Lista de Clicks</h1>

<table>
  <tr>
    <th>Direccion IP</th>
    <th>Fecha</th>
    <th>Advertencia</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clicks.each do |click| %>
  <tr>
    <td><%= click.ip_address %></td>
    <td><%= click.date %></td>
    <td><%= click.advertisement %></td>
    <td><%= link_to 'Mostrar', click %></td>
    <td><%= link_to 'Editar', edit_click_path(click) %></td>
    <td><%= button_to 'Eliminar', click, :confirm => 'Esta seguro?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo Clic', new_click_path %>
