
<h1>eventos</h1>

<table>
  <tr>
    <th>Descripcion Evento</th>
    <th>Fecha Inicio</th>
    <th>Fecha Fin</th>
    <th>Titulo</th>
    <th>pagina web</th>
    <th>Usuario</th>
    <th>       </th>
    <th>       </th>
    <th>       </th>
  </tr>



<% Event.find_by_sql("SELECT a.*, b.email FROM events a
inner join users b ON a.user_id = b.id" ).each do |event| %>


  <tr>
    <td><%= event.description %></td>
    <td><%= event.start_date %></td>
    <td><%= event.final_date %></td>
    <td><%= event.title %></td>
    <td><%= event.link %></td>
    <td><%= event.email %></td>
    <td><%= link_to 'Mostrar', event %></td>
    <td><%= link_to 'Editar', edit_event_path(event) %></td>
    <td><%= button_to 'Borrar', event, :confirm => 'Seguro?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo evento', new_event_path %>


<br />

